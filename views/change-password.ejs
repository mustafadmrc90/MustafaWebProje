<!DOCTYPE html>
<html lang="tr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Şifre Değiştir</title>
    <link rel="stylesheet" href="/public/styles.css" />
  </head>
  <body class="auth-page">
    <div class="auth-card">
      <div class="auth-header">
        <div class="badge">Dashboard</div>
        <h1>Şifre Değiştir</h1>
        <p>Mevcut şifrenizi doğrulayın ve yeni şifre belirleyin.</p>
      </div>

      <% if (error) { %>
        <div class="alert"><%= error %></div>
      <% } %>

      <% if (ok) { %>
        <div class="alert success">Şifre başarıyla güncellendi.</div>
      <% } %>

      <form method="post" action="/change-password" class="auth-form">
        <label>
          <span>Mevcut Şifre</span>
          <input type="password" name="currentPassword" autocomplete="current-password" required />
        </label>
        <label>
          <span>Yeni Şifre</span>
          <input type="password" name="newPassword" autocomplete="new-password" required />
        </label>
        <label>
          <span>Yeni Şifre (Tekrar)</span>
          <input type="password" name="confirmPassword" autocomplete="new-password" required />
        </label>
        <button type="submit">Güncelle</button>
      </form>

      <div class="auth-footer">
        <a class="button-link ghost" href="/dashboard">Dashboard’a Dön</a>
      </div>
    </div>
  </body>
</html>
