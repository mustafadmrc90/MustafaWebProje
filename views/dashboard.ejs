<!DOCTYPE html>
<html lang="tr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard</title>
    <link rel="stylesheet" href="/public/styles.css" />
  </head>
  <body class="dashboard layout">
    <%- include("partials/sidebar", { active }) %>

    <section class="content">
      <header class="page-header">
        <div class="page-title">
          <h1>Obus API</h1>
          <p>API uçlarını ve ortam hedeflerini tek panelden yönetin.</p>
        </div>
        <div class="page-actions">
          <div class="user-pill"><%= user.displayName %></div>
          <button class="ghost">Yenile</button>
        </div>
      </header>

      <div class="api-toolbar">
        <div class="api-target">
          <span class="label">Hedef URL</span>
          <select class="target-select">
            <option>Seçiniz</option>
          </select>
        </div>
        <div class="api-actions">
          <button class="button-link" id="open-endpoint-modal">Endpoint Ekle</button>
          <button class="ghost">Yenile</button>
        </div>
      </div>

      <div class="api-layout">
        <aside class="api-sidebar">
          <div class="api-sidebar-header">
            <h2>Endpointler</h2>
            <span class="pill">0 seçili</span>
          </div>
          <div class="api-list" id="endpoint-list"></div>
        </aside>

        <section class="api-panel">
          <div class="panel-card">
            <div class="endpoint-header">
              <div>
                <h2 id="endpoint-title">GetSession</h2>
                <p class="muted" id="endpoint-path">POST /GetSession</p>
              </div>
              <span class="pill" id="endpoint-method">POST</span>
            </div>

            <div class="request-tabs">
              <button class="tab active" data-tab="body">Body</button>
              <button class="tab" data-tab="headers">Headers</button>
              <button class="tab" data-tab="params">Params</button>
            </div>

            <div class="request-body tab-panel active" data-panel="body">
              <label class="label">Body (JSON)</label>
              <textarea id="endpoint-body" class="code-input" rows="12"></textarea>
            </div>

            <div class="request-body tab-panel" data-panel="headers">
              <label class="label">Headers</label>
              <textarea id="endpoint-headers" class="code-input" rows="8" placeholder='{"Content-Type":"application/json"}'></textarea>
            </div>

            <div class="request-body tab-panel" data-panel="params">
              <label class="label">Params</label>
              <textarea id="endpoint-params" class="code-input" rows="6" placeholder='{"page":1,"size":10}'></textarea>
            </div>
          </div>
        </section>
      </div>
    </section>

    <div class="modal-backdrop" id="endpoint-modal" aria-hidden="true">
      <div class="modal">
        <div class="modal-header">
          <h2>Endpoint Ekle</h2>
          <button class="ghost small" id="close-endpoint-modal">Kapat</button>
        </div>
        <form id="endpoint-form" class="modal-form">
          <label>
            <span>Başlık</span>
            <input type="text" name="title" placeholder="Firma Listeleme" required />
          </label>
          <label>
            <span>Method</span>
            <select name="method">
              <option value="GET">GET</option>
              <option value="POST">POST</option>
              <option value="PUT">PUT</option>
              <option value="DELETE">DELETE</option>
            </select>
          </label>
          <label>
            <span>Endpoint URL</span>
            <input type="text" name="path" placeholder="/company/list" required />
          </label>
          <label>
            <span>Açıklama</span>
            <input type="text" name="description" placeholder="Opsiyonel" />
          </label>
          <button type="submit" class="button-link">Kaydet</button>
        </form>

        <div class="modal-list">
          <h3>Eklenen Endpointler</h3>
          <div class="endpoint-table" id="endpoint-table"></div>
        </div>
      </div>
    </div>
    <script src="/public/app.js" defer></script>
  </body>
</html>
