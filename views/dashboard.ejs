<!DOCTYPE html>
<html lang="tr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard</title>
    <link rel="stylesheet" href="/public/styles.css" />
  </head>
  <body class="dashboard layout">
    <%- include("partials/sidebar", { active }) %>

    <section class="content">
      <header class="page-header">
        <div class="page-title">
          <h1>Obus API</h1>
          <p>API uçlarını ve ortam hedeflerini tek panelden yönetin.</p>
        </div>
        <div class="page-actions">
          <div class="user-pill"><%= user.displayName %></div>
          <button class="ghost">Yenile</button>
        </div>
      </header>

      <% if (loginSuccess) { %>
        <div class="alert success inline-alert">Başarıyla giriş yapıldı.</div>
      <% } %>

      <div class="api-toolbar">
        <div class="api-target">
          <span class="label">Hedef URL</span>
          <select class="target-select">
            <option>Prod (otomatik)</option>
            <option>Stage (otomatik)</option>
            <option>Test (otomatik)</option>
          </select>
        </div>
        <div class="api-actions">
          <button class="button-link">Endpoint Ekle</button>
          <button class="ghost">Yenile</button>
        </div>
      </div>

      <div class="api-layout">
        <aside class="api-sidebar">
          <div class="api-sidebar-header">
            <h2>Endpointler</h2>
            <span class="pill">0 seçili</span>
          </div>
          <div class="api-list">
            <button class="api-item active">
              <span class="method get">GET</span>
              <span>/company/list</span>
            </button>
            <button class="api-item">
              <span class="method post">POST</span>
              <span>/company/create</span>
            </button>
            <button class="api-item">
              <span class="method get">GET</span>
              <span>/reports/summary</span>
            </button>
            <button class="api-item">
              <span class="method put">PUT</span>
              <span>/permissions/update</span>
            </button>
          </div>
        </aside>

        <section class="api-panel">
          <div class="panel-card">
            <h2>Endpoint Detayı</h2>
            <p class="muted">
              Sol listeden bir endpoint seçildiğinde detaylar burada güncellenecek.
            </p>
            <div class="empty-state">
              <div class="empty-icon">API</div>
              <p>Endpoint detayları ve yanıtları burada görünecek.</p>
            </div>
          </div>
        </section>
      </div>
    </section>
    <script src="/public/app.js" defer></script>
  </body>
</html>
