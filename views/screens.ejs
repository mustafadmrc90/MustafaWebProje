<!DOCTYPE html>
<html lang="tr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Bus Product</title>
    <link rel="icon" href="/public/favicon.png" />
    <link rel="stylesheet" href="/public/styles.css" />
  </head>
  <body class="dashboard layout">
    <%- include("partials/sidebar", { active }) %>

    <section class="content">
      <header class="page-header">
        <div class="page-title">
          <h1>Ekranlar</h1>
          <p>Dashboard modüllerini ve ekranlarını tanımlayın.</p>
        </div>
        <div class="page-actions">
          <div class="user-pill"><%= user.displayName %></div>
        </div>
      </header>

      <% if (ok) { %>
        <div class="alert success inline-alert">Ekran eklendi.</div>
      <% } %>

      <div class="panel-grid">
        <section class="table-card panel-card">
          <h2>Yeni Ekran</h2>
          <form class="form-grid" method="post" action="/screens">
            <label>
              <span>Anahtar</span>
              <input type="text" name="key" placeholder="reports" required />
            </label>
            <label>
              <span>Ad</span>
              <input type="text" name="name" placeholder="Raporlar" required />
            </label>
            <button type="submit">Ekran Ekle</button>
          </form>
        </section>

        <section class="table-card panel-card">
          <div class="table-head">
            <h2>Mevcut Ekranlar</h2>
          </div>
          <div class="table">
            <div class="row head two">
              <span>Anahtar</span>
              <span>Ad</span>
            </div>
            <% screens.forEach((s) => { %>
              <div class="row two">
                <span><%= s.key %></span>
                <span><%= s.name %></span>
              </div>
            <% }) %>
          </div>
        </section>
      </div>
    </section>
    <script src="/public/app.js" defer></script>
  </body>
</html>
