<!DOCTYPE html>
<html lang="tr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Kullanıcılar</title>
    <link rel="stylesheet" href="/public/styles.css" />
  </head>
  <body class="dashboard">
    <header class="topbar">
      <div>
        <div class="badge">Dashboard</div>
        <h1>Kullanıcılar</h1>
        <p class="muted">Yetki düzenlemeleri için kullanıcı seçin.</p>
      </div>
      <div class="actions">
        <a class="button-link ghost" href="/dashboard">Dashboard</a>
        <form method="post" action="/logout">
          <button class="ghost" type="submit">Çıkış</button>
        </form>
      </div>
    </header>

    <main class="grid">
      <section class="card span-2">
        <h2>Yeni Kullanıcı</h2>
        <form class="form-grid" method="post" action="/users">
          <label>
            <span>Kullanıcı Adı</span>
            <input type="text" name="username" required />
          </label>
          <label>
            <span>Görünen İsim</span>
            <input type="text" name="displayName" required />
          </label>
          <label>
            <span>Şifre</span>
            <input type="password" name="password" required />
          </label>
          <button type="submit">Kullanıcı Ekle</button>
        </form>
        <% if (ok) { %>
          <div class="alert success">Kullanıcı oluşturuldu.</div>
        <% } %>
      </section>

      <section class="card span-2">
        <h2>Mevcut Kullanıcılar</h2>
        <div class="table">
          <div class="row head">
            <span>Kullanıcı</span>
            <span>Görünen İsim</span>
            <span>İşlemler</span>
          </div>
          <% users.forEach((u) => { %>
            <div class="row">
              <span>@<%= u.username %></span>
              <span><%= u.display_name %></span>
              <span>
                <a class="button-link ghost" href="/permissions/<%= u.id %>">Yetkiler</a>
              </span>
            </div>
          <% }) %>
        </div>
      </section>
    </main>
  </body>
</html>
